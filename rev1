import numpy as np
import matplotlib.pyplot as plt 
from scipy.optimize import fsolve   
import sympy as sp 
from sympy import Derivative
from sympy import Integral
x, y, z = sp.symbols('x y z') 
choice = int(input("Please choose a scenario number: \nFinite Well (E < U_o) [1] \nInfinite Well [2]\nHarmonic Oscillator [3]\n"))
print("Next, you will enter parameters. A couple of tips while choosing these parameters: \n1. Potential energy should be in the eV range (1 eV = 1.602*10^(-19) Joules).\n2. Mass should be on a quantum scale (similar to the mass of an electron). ")
mass = sp.sympify(input("Enter mass: "))
dimensions = sp.sympify(float(input("Enter dimensions: ")))
domainL = sp.sympify(float(input("Enter lower domain: ")))
domainU = sp.sympify(float(input("Enter upper domain: ")))
potential_energy = sp.sympify(float(input("Enter potential energy: ")))
if choice == 1:  
    print("test")
elif choice == 2:
    print("test1")
elif choice == 3: 
    print("test2")
elif choice != 1 | 2 | 3: 
    print("Please enter a valid scenario input(1, 2, or 3).")
